#include<iostream>
using namespace std;

int main ()
{
/*
    // If a no. is +ve or -ve not 0
    double a;
    cin>>a;
    // cout << "The No. is: " << n << endl;

    if(a > 0) {
        cout << "the number " << a << " is +ve" << endl; 
    }
    else{
        cout << "the number " << a << " is -ve." << endl;         
    }
    
    // Compare a & b
    int a,b;

    cout << "Enter the value of a : " << endl;
    cin >> a;

    cout << "Enter the value of b : " << endl;
    cin >> b;
    
    if(a > b) {
        cout << "the number " << a << " is " << "greater than " << b << endl;
    }
    if(a < b){
        cout << "the number " << a << " is " << "lesser than " << b << endl;
    }

    // If a no. is -ve,0 or +ve
    double a;
    cout << "Enter the value of a :" << endl;
    cin >> a;

    if(a > 0) {
        cout << "the number " << a << " is +ve" << endl; 
    }
    else if(a==0){
        cout << "the number " << a << " is 0." << endl;         
    }
    else{
        cout << "the number " << a << " is -ve." << endl;         
    }

    // H.W. Tells if char entered is a digit, an uppercase letter or lowercase letter.
    cout << "Enter any letter or a digit" << endl;
    char ch;
    cin>>ch;

    cout << ch << endl;
    if( ch >= 65 && ch <= 90 ){
        cout << "uppercase" << endl;
    }
    else if ( ch >= 97 && ch <= 122 ){
        cout << "lowercase" << endl;
    }
    else if ( ch >= 48 && ch <= 57 ){
        cout << "it is a digit" << endl;
    }
    else{
        cout << "enter a digit like 9,8,7 etc. or a letter like a,b,c etc. or A,B,C etc." << endl;
    }
    
*/
    double n=1;
    double a;
    cout << "Enter the value of a" << endl;
    cin >> a;
    while (n <= a){
        cout << n << endl;
        n++;
    }


}